<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingPrefix"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="0dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/home_features_header"
        android:textSize="16sp"
        android:textColor="?android:attr/textColorPrimary"
        android:paddingLeft="@dimen/home_content_padding"
        android:paddingStart="@dimen/home_content_padding"
        android:paddingRight="@dimen/home_content_padding"
        android:paddingEnd="@dimen/home_content_padding"
        android:layout_marginBottom="@dimen/home_content_padding"
        fontPath="fonts/Font-Bold.ttf" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/divider_list"
        android:visibility="gone" />

</LinearLayout>